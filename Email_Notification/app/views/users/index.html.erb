<% if @users != nil %>
  <% if @users.first.id ==current_user.id %>
  You are the Admin
    <%= form_tag mail_path({}) do %>
      <br>
        <table id="customers">
          <th> Name </th>
          <th> Location</th>
          <th> Email-Id </th>
          <th> Select  </th>

        <% @users.each do |user| %>

        <tr>

            <td><%= user.name%></td>

            <td><%= user.location%></td>

            <td><%= user.email %></td>

            <td>
                 <%= check_box_tag 'user_ids[]', user.id %>
            </td>
        </tr>


        <% end %>

        </table>
        <center>

        <div><%= submit_tag 'Send Email to Selected users' %></div>
        </center>
    <% end %>
 <%= link_to 'Send Email to All Users', sendall_path, class: 'button'%><br>
  <%= link_to 'Send Email based on their location', sendlocation_path, class: 'button'%>
  <% else %>
  <header class="masthead">
        <div class="container h-100">
            <div class="row h-100 align-items-center justify-content-center text-center">
                <div class="col-lg-10 align-self-end">
                    <h1 class="text-uppercase text-white font-weight-bold">Welcome, <%= current_user.name %></h1>
                    <hr class="divider my-4" />
                </div>
                <div class="col-lg-8 align-self-baseline">
                    <p class="text-white-75 font-weight-light mb-5">You are registered as a user in Mailer</p>
                </div>
            </div>
        </div>
    </header>
  <% end %>
<% end %>
